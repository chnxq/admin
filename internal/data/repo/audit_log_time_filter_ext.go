// Code generated by: codex.
// generated at        2026-05-09 23:00:00 +08:00.

package repo

import (
	"fmt"
	"strconv"
	"strings"
	"time"

	paginationv1 "github.com/chnxq/x-crud/api/gen/pagination/v1"
)

func parseFilterTime(value, field string) (time.Time, error) {
	trimmed := strings.TrimSpace(value)
	if trimmed == "" {
		return time.Time{}, fmt.Errorf("invalid %s filter value: empty", field)
	}

	for _, layout := range []string{
		time.RFC3339Nano,
		time.RFC3339,
		"2006-01-02 15:04:05",
		"2006-01-02",
	} {
		if parsed, err := time.Parse(layout, trimmed); err == nil {
			return parsed, nil
		}
	}

	if unixSeconds, err := strconv.ParseInt(trimmed, 10, 64); err == nil {
		return time.Unix(unixSeconds, 0), nil
	}

	return time.Time{}, fmt.Errorf("invalid %s filter value: %q", field, value)
}

func parseFilterTimeRange(values []string, field string) (time.Time, time.Time, error) {
	if len(values) < 2 {
		return time.Time{}, time.Time{}, fmt.Errorf("invalid %s range filter value", field)
	}

	start, err := parseFilterTime(values[0], field)
	if err != nil {
		return time.Time{}, time.Time{}, err
	}
	end, err := parseFilterTime(values[1], field)
	if err != nil {
		return time.Time{}, time.Time{}, err
	}
	return start, end, nil
}

func applyApiAuditCreatedAtFilters(req *paginationv1.PagingRequest) error {
	if req == nil || req.GetFilterExpr() == nil {
		return nil
	}

	conditions := req.GetFilterExpr().GetConditions()
	filtered := make([]*paginationv1.FilterCondition, 0, len(conditions))
	for _, condition := range conditions {
		if condition == nil || condition.GetField() != "created_at" {
			filtered = append(filtered, condition)
			continue
		}

		switch condition.GetOp() {
		case paginationv1.Operator_GT, paginationv1.Operator_GTE, paginationv1.Operator_LT, paginationv1.Operator_LTE:
			_, err := parseFilterTime(condition.GetValue(), "created_at")
			if err != nil {
				return err
			}
			filtered = append(filtered, condition)
		case paginationv1.Operator_BETWEEN:
			start, end, err := parseFilterTimeRange(condition.GetValues(), "created_at")
			if err != nil {
				return err
			}
			filtered = append(filtered,
				&paginationv1.FilterCondition{
					Field:      "created_at",
					Op:         paginationv1.Operator_GTE,
					ValueOneof: &paginationv1.FilterCondition_Value{Value: start.Format(time.RFC3339Nano)},
				},
				&paginationv1.FilterCondition{
					Field:      "created_at",
					Op:         paginationv1.Operator_LTE,
					ValueOneof: &paginationv1.FilterCondition_Value{Value: end.Format(time.RFC3339Nano)},
				},
			)
		default:
			filtered = append(filtered, condition)
		}
	}
	req.GetFilterExpr().Conditions = filtered
	return nil
}

func applyLoginAuditCreatedAtFilters(req *paginationv1.PagingRequest) error {
	if req == nil || req.GetFilterExpr() == nil {
		return nil
	}

	conditions := req.GetFilterExpr().GetConditions()
	filtered := make([]*paginationv1.FilterCondition, 0, len(conditions))
	for _, condition := range conditions {
		if condition == nil || condition.GetField() != "created_at" {
			filtered = append(filtered, condition)
			continue
		}

		switch condition.GetOp() {
		case paginationv1.Operator_GT, paginationv1.Operator_GTE, paginationv1.Operator_LT, paginationv1.Operator_LTE:
			_, err := parseFilterTime(condition.GetValue(), "created_at")
			if err != nil {
				return err
			}
			filtered = append(filtered, condition)
		case paginationv1.Operator_BETWEEN:
			start, end, err := parseFilterTimeRange(condition.GetValues(), "created_at")
			if err != nil {
				return err
			}
			filtered = append(filtered,
				&paginationv1.FilterCondition{
					Field:      "created_at",
					Op:         paginationv1.Operator_GTE,
					ValueOneof: &paginationv1.FilterCondition_Value{Value: start.Format(time.RFC3339Nano)},
				},
				&paginationv1.FilterCondition{
					Field:      "created_at",
					Op:         paginationv1.Operator_LTE,
					ValueOneof: &paginationv1.FilterCondition_Value{Value: end.Format(time.RFC3339Nano)},
				},
			)
		default:
			filtered = append(filtered, condition)
		}
	}
	req.GetFilterExpr().Conditions = filtered
	return nil
}

func applyPermissionAuditCreatedAtFilters(req *paginationv1.PagingRequest) error {
	if req == nil || req.GetFilterExpr() == nil {
		return nil
	}

	conditions := req.GetFilterExpr().GetConditions()
	filtered := make([]*paginationv1.FilterCondition, 0, len(conditions))
	for _, condition := range conditions {
		if condition == nil || condition.GetField() != "created_at" {
			filtered = append(filtered, condition)
			continue
		}

		switch condition.GetOp() {
		case paginationv1.Operator_GT, paginationv1.Operator_GTE, paginationv1.Operator_LT, paginationv1.Operator_LTE:
			_, err := parseFilterTime(condition.GetValue(), "created_at")
			if err != nil {
				return err
			}
			filtered = append(filtered, condition)
		case paginationv1.Operator_BETWEEN:
			start, end, err := parseFilterTimeRange(condition.GetValues(), "created_at")
			if err != nil {
				return err
			}
			filtered = append(filtered,
				&paginationv1.FilterCondition{
					Field:      "created_at",
					Op:         paginationv1.Operator_GTE,
					ValueOneof: &paginationv1.FilterCondition_Value{Value: start.Format(time.RFC3339Nano)},
				},
				&paginationv1.FilterCondition{
					Field:      "created_at",
					Op:         paginationv1.Operator_LTE,
					ValueOneof: &paginationv1.FilterCondition_Value{Value: end.Format(time.RFC3339Nano)},
				},
			)
		default:
			filtered = append(filtered, condition)
		}
	}
	req.GetFilterExpr().Conditions = filtered
	return nil
}
